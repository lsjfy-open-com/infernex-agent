## 行为变化

<!-- 用一个具体触发条件说明改动前后，以及本 PR 的单一可验收结果。 -->

## 范围与依赖

- 主要范围：`area/deployment` / `area/operations` / `area/core` / `area/experience`（保留适用项）
- Base 与依赖 PR：无依赖时填写 `main`；堆叠 PR 写明直接依赖和合入顺序。

## 验证

<!-- 写实际运行的命令、结果和环境；列出能证明行为变化的正常/失败场景。 -->

- 已完成：
- 尚未验证或已知限制：

## 权限与回退

- 权限/执行通道变化：无变化写“无”；有变化说明对象、范围与批准边界。
- 写操作的前置检查、失败回退与回退失败处理：不涉及写操作写“不涉及”。
- 撤销本改动与持久状态兼容性：无运行时影响写“无”。

<!-- Draft 集成候选仍须满足其合入/发版门槛；本地测试通过或创建恢复候选成功，不等于现场验收或服务恢复完成。 -->
