# openEuler ??/?????????

??????? InferNex Agent ???? Linux systemd ?????
Kubernetes master ????????????????? Pod?

## 1. A2 ? openEuler ????????

Atlas A2 ? NPU/???????????? ?A2? ?????? CPU ???
?????????

```bash
uname -m
cat /etc/os-release
```

???????

| `uname -m` | Agent ? |
|---|---|
| `x86_64` | `linux-amd64` |
| `aarch64` | `linux-arm64` |

Agent ? `CGO_ENABLED=0` ??? Go ??????? openEuler ? glibc
??????? CANN?NPU ??????????? Kubernetes API ?
????? OpenAI ?????????? ?openEuler ?? Agent ????
????? CPU ?????????? systemd ?????

## 2. ???????????

| ?? | ??? Helm | master/???? systemd |
|---|---|---|
| ???? | Kubernetes Pod | ?? Linux ?? |
| ???? | ??? ServiceAccount Token | ?? kubeconfig |
| ???? | Deployment/Helm | systemd |
| Dashboard | Service/NodePort | ?????????? |
| ???? | Kubernetes ???? | ????????????????? |
| InferNex ?? | ??? Bridge ?? | ??? Bridge ?? |

??????????? Agent ?????????????????
Kubernetes?Agent ??? apiserver ?? `InferNexService`?Pod?Event
????????????????????????????

?????????????????????????????????
???????????????????????????????????

## 3. ??????

?? Linux ????? Go 1.24+?Bash?`tar` ? `sha256sum`?

```bash
cd component/InferNex-Agent

./scripts/offline/build-host-bundle.sh \
  --version 0.3.0 \
  --architecture arm64 \
  --output-dir ./dist
```

???

```text
infernex-agent-host-offline-0.3.0-linux-arm64.tar.gz
infernex-agent-host-offline-0.3.0-linux-arm64.tar.gz.sha256
```

?? CI ????????????

```bash
./scripts/offline/build-host-bundle.sh \
  --version 0.3.0 \
  --architecture arm64 \
  --binary /secure/build/infernex-agent-linux-arm64 \
  --output-dir ./dist
```

????????????systemd ????????? kubeconfig
????????????????????????

## 4. ???????

```bash
sha256sum --check \
  infernex-agent-host-offline-0.3.0-linux-arm64.tar.gz.sha256

tar -xzf infernex-agent-host-offline-0.3.0-linux-arm64.tar.gz
cd infernex-agent-host-offline-0.3.0-linux-arm64

./bin/verify-host.sh --help
```

??????????? `SHA256SUMS` ??? CPU ???

## 5. ???????????

??? systemd ?????? `/etc/kubernetes/admin.conf`??????
???? Agent ????????

????? kubeconfig ????? namespace-scoped ServiceAccount?
Role/RoleBinding ????? kubeconfig?

```bash
sudo ./bin/create-kubeconfig.sh \
  --admin-kubeconfig /etc/kubernetes/admin.conf \
  --target-namespace model-a \
  --target-namespace model-b \
  --output /root/infernex-agent-host.kubeconfig
```

??????????????

- `get/list` InferNexService?
- `list` Deployment?DaemonSet?LeaderWorkerSet?Pod ? Event?

??????????????????

```bash
--enable-recovery \
--recovery-template-namespace infernex-bridge-system
```

?????

- ???????? `InferNexService`?
- ???????????? `InferNexServiceConfig`?

???? catalog ??/??????? `--enable-deployment`?????
cluster-admin??????? Secret?Node ????? Deployment?

???????????????

```bash
--enable-experiments \
--experiment-template-namespace infernex-bridge-system
```

??????????????? `pods/log get`???
`InferNexService create/delete` ??? profile ??????

?? kubeconfig ???? ServiceAccount Token????????????

- ?????? `0600`?
- ??????????
- ??????????????
- ????????? PKI/OIDC ????????? kubeconfig?

## 6. ? openEuler ??? systemd ??

### 6.1 ?????

?? MCP ? Dashboard ???? loopback?

```bash
sudo ./bin/install-host.sh \
  --kubeconfig /root/infernex-agent-host.kubeconfig \
  --scan-namespace model-a \
  --scan-namespace model-b
```

?????

```text
MCP:       http://127.0.0.1:8080/mcp
Dashboard: http://127.0.0.1:8081/
```

??? kubectl-ai?Codex runtime ??? Agent Runtime ??????
????????

### 6.2 ????????? Dashboard

??????????? IP?????????

```bash
sudo ./bin/install-host.sh \
  --kubeconfig /root/infernex-agent-host.kubeconfig \
  --scan-namespace model-a \
  --dashboard-listen-address 10.20.0.10:8081
```

????????????openEuler ?? firewalld ?????

```bash
sudo firewall-cmd --permanent \
  --add-rich-rule='rule family=ipv4 source address=10.20.0.0/16 port port=8081 protocol=tcp accept'
sudo firewall-cmd --reload
```

Dashboard ?????????????????????????????
`0.0.0.0/0` ???MCP ????? `127.0.0.1:8080`????? Agent
Runtime ???????????????????? `--listen-address`?

?????????????SELinux ???SSH ????????

## 7. ??????

????????????????????????????????MCP?
Dashboard ? JSON ?????????????????????????

??????

```bash
sudo ./bin/install-host.sh \
  --kubeconfig /root/infernex-agent-host.kubeconfig \
  --scan-namespace model-a \
  --dashboard-listen-address 10.20.0.10:8081 \
  --openai-base-url http://10.20.0.30:8000/v1 \
  --openai-model ops-diagnostic-model \
  --openai-api-key-file /root/infernex-openai.key \
  --openai-timeout 60s
```

API Key ???? `/etc/infernex-agent/openai-api-key`?????
`infernex-agent` ????? `0600`?Key ??? systemd unit??????
Bundle ? Dashboard?

????????????????? Key?

```bash
sudo /opt/infernex-agent/bin/configure-model.sh \
  --base-url http://10.20.0.30:8000/v1 \
  --model ops-diagnostic-model \
  --api-key-file /root/infernex-openai.key \
  --timeout 60s \
  --test \
  --show
```

???????? Agent?

```bash
sudo /opt/infernex-agent/bin/configure-model.sh --disable --show
```

?????? `/etc/infernex-agent/agent.conf`?API Key ?????????
?????????? `--openai-*` ??????????????????
[??????](model-configuration-zh.md)?

## 8. ??????

?? kubeconfig ?????????????

```bash
sudo ./bin/install-host.sh \
  --kubeconfig /root/infernex-agent-host.kubeconfig \
  --scan-namespace model-a \
  --enable-recovery \
  --recovery-template-namespace infernex-bridge-system
```

??????

1. ?? `InferNexServiceConfig` ?
   `agent.infernex.io/approved-recovery-profile=true`?
2. ???? `agent.infernex.io/auto-recovery=true` ??? profile ??
3. ?????????? Bridge ??? `observedGeneration`?

Agent ??????????? `InferNexService`?InferNex Bridge ??
?? PD/???????Service?????????

## 9. ???????

```bash
sudo systemctl status infernex-agent --no-pager
sudo journalctl -u infernex-agent -f

sudo ./bin/verify-host.sh \
  --kubeconfig /etc/infernex-agent/kubeconfig \
  --target-namespace model-a
```

???????

```bash
sudo ss -lntp | grep infernex-agent
```

????????????

```text
/opt/infernex-agent/bin/       ????????????????
/etc/infernex-agent/agent.conf ???????
/etc/infernex-agent/kubeconfig ?? Kubernetes ??
/etc/infernex-agent/openai-api-key  ??????
/var/lib/infernex-agent/       systemd ????
/etc/systemd/system/           infernex-agent.service
```

systemd unit ???? root ??????????? capability ??
`NoNewPrivileges`?????????????openEuler ?? SELinux ??
?????? `restorecon`??????????????????????
?? AVC ???????? SELinux?

## 10. ??????????

?????????? `install-host.sh` ?????????????????
? `/var/lib/infernex-agent/backups/install-*/` ?????????????
??????? systemd ????????????????????????
????????????????????????????????????
??????????

```text
/opt/infernex-agent/bin/infernex-agent.previous
```

?????

```bash
sudo systemctl stop infernex-agent
sudo cp /opt/infernex-agent/bin/infernex-agent.previous \
  /opt/infernex-agent/bin/infernex-agent
sudo restorecon -F /opt/infernex-agent/bin/infernex-agent 2>/dev/null || true
sudo systemctl start infernex-agent
```

???????????????

```bash
sudo /opt/infernex-agent/bin/infernex-agent cluster-state verify \
  --input /var/lib/infernex-agent/backups/install-*/cluster-state.json

sudo /opt/infernex-agent/bin/infernex-agent cluster-state restore \
  --kubeconfig /etc/infernex-agent/kubeconfig \
  --input /var/lib/infernex-agent/backups/install-20260731T010203Z-1234/cluster-state.json \
  --confirm
```

???????????[??????????](change-safety-zh.md)?

???????????? systemd ???

```bash
sudo /opt/infernex-agent/bin/restore-host-install.sh \
  --backup-dir /var/lib/infernex-agent/backups/install-20260731T010203Z-1234 \
  --confirm
```

?? ServiceAccount Token?

```bash
sudo ./bin/create-kubeconfig.sh \
  --admin-kubeconfig /etc/kubernetes/admin.conf \
  --target-namespace model-a \
  --output /root/infernex-agent-host.kubeconfig \
  --rotate-token \
  --force

sudo ./bin/install-host.sh \
  --kubeconfig /root/infernex-agent-host.kubeconfig \
  --scan-namespace model-a
```

## 11. ??

```bash
sudo ./bin/uninstall-host.sh
```

???? `/etc/infernex-agent` ???`/var/lib/infernex-agent` ????/
???? Kubernetes RBAC??????????????????????
????????

```bash
sudo ./bin/uninstall-host.sh --purge-credentials --purge-state --purge-user
```

```bash
kubectl -n infernex-system delete \
  serviceaccount/infernex-agent-host \
  secret/infernex-agent-host-token
```

?????????????? `infernex-agent-host` ?
`infernex-agent-host-mutation` ? Role/RoleBinding???????
InferNexService?Bridge??? Pod??? PVC ? NPU ???
