# InferNex Agent ??????????

## 1. ????

????? Agent ?????????????????????

1. ????????????? Agent ?????????? systemd unit
   ??????????????
2. ???? catalog ?????????????????????????
   Ready?? InferNex ??????? generation ??? `Degraded`?Agent
   ???????????
3. ????????????????????????????? Ready?
   Degraded ????????????????????????????????

?????????? Agent ??????????
???????? `InferNexService`?Deployment?DaemonSet?Pod ? Service
? InferNex Bridge ?? CR ????????? CR ?? Bridge ?????
Agent ???????????????

??? etcd ???????????????????????????????

## 2. ?????????

`install-host.sh` ?? Kubernetes ????????????????????

1. ?????????????? `--scan-namespace`?
2. ???? `InferNexService` ??? API ???UID?resourceVersion ??????
3. ??????? SHA-256?
4. ????????`.previous`?????`agent.conf`?kubeconfig???
   API Key?????? systemd unit?
5. ???????? active/enabled ???

??????

```text
/var/lib/infernex-agent/backups/install-<UTC??>-<PID>/
??? cluster-state.json
??? cluster-state.sha256
??? host/
    ??? manifest
    ??? checksums.sha256
    ??? <??????>
```

??? root ?? `0700`???? `0600`??????? kubeconfig ???
API Key?????????????????????????????

?????systemd ????????????? trap ????

- ?????????
- ???????????????????????????????????
- ?? systemd ? enabled/active ???
- ????????????? Agent `change-id` ??????
- ?????????????????

????????????

## 3. ???????

?????????????

```bash
sudo /opt/infernex-agent/bin/infernex-agent cluster-state verify \
  --input /var/lib/infernex-agent/backups/install-*/cluster-state.json
```

?????

```bash
sudo /opt/infernex-agent/bin/infernex-agent cluster-state restore \
  --kubeconfig /etc/infernex-agent/kubeconfig \
  --input /var/lib/infernex-agent/backups/install-20260731T010203Z-1234/cluster-state.json \
  --confirm
```

??????????????? systemd ?????????????????

```bash
sudo /opt/infernex-agent/bin/restore-host-install.sh \
  --backup-dir /var/lib/infernex-agent/backups/install-20260731T010203Z-1234 \
  --confirm
```

??????? Agent ?????????????????????????
`/var/lib/infernex-agent` ?????????????????
`--purge-state` ?????

????????????

- ????????????? `app.kubernetes.io/managed-by=infernex-agent`
  ? `agent.infernex.io/change-id` ????
- ????????? Agent ?????????????
- ???? Agent ????? `change-id` ????
- ??????????????????? spec?????????
  `skipped`????????????

## 4. ?????????

`infernex_deploy_model` ?????????

```text
?? planned ??
  ? ??? change-id ? InferNexService
  ? ?? applied ??
  ? ???? status
      ?? Ready ? observedGeneration >= generation ? committed
      ?? ??/?? generation Degraded
           ? ?????? change-id
           ? ????????
           ? ???????
           ? rolled-back
```

?? Ready ??? `10m`??????????

```bash
sudo ./bin/install-host.sh \
  --kubeconfig /root/infernex-agent-host.kubeconfig \
  --scan-namespace models \
  --enable-deployment \
  --deployment-readiness-timeout 15m
```

???????? `changeId` ? `changeStatus=applied`??????????

```json
{
  "name": "infernex_get_change",
  "arguments": {
    "changeId": "32?????ID"
  }
}
```

????

- `committed`?????? InferNex Ready ???
- `rolled-back`??????????????????????
- `rollback-failed`?????????API ?????????????????????
- `apply-failed`??????????????????

?????????????
`/var/lib/infernex-agent/changes/<change-id>/`?Agent ??????????
??? `planned` ? `applied` ????????????????????

??????????????? `planned`??????? `committed`????
?? `change-id`????????????????????????????
??????? ID ????? Dashboard/JSON ? remediation ????

????????????? `changeId`?????????
`/var/lib/infernex-agent/experiments/`?????? ID???????profile ?
`changeId`????????????????????? `committed` ?????
??????????? `rolled-back` ? `rollback-failed`?Agent ?????
??? planned/running ?????????????? spec ????????

## 5. Kubernetes/Helm ??

Helm ??????? `--atomic --wait`?Agent ???????? Helm ??
?? release ???????????

?? catalog ?????????????????????????

```bash
./bin/install-agent.sh \
  --target-namespace models \
  --dashboard-cidr 10.20.0.0/16 \
  --enable-deployment \
  --deployment-readiness-timeout 10m \
  --state-storage-class local-path
```

???????????? PVC?

```bash
--state-existing-claim infernex-agent-state
```

???????????????
`changeSafety.persistence.enabled=true`????? Helm ????????

```yaml
tools:
  deployment:
    enabled: true
changeSafety:
  persistence:
    enabled: true
    existingClaim: infernex-agent-state
```

Kind ???????? `emptyDir`??????????? Pod ???????
??????????????????

## 6. ????

??????

- ???????
- ???????? Agent ????? Agent ??????????
- ?? catalog ??????????????
- Agent ???????????
- ??? `change-id`??????????????
- ??????????????????????????????????

??????

- etcd?Secret?PVC ????? Kubernetes ??????
- ??????????????? spec?
- ???????????????????????????????????
  ????????????
- ??????????????
- ???????????????????????? generation?Ready?
  Degraded ??????

## 7. ????

- [ ] ?????? `pre-install recovery point`?
- [ ] `cluster-state verify` ?? `valid: true`?
- [ ] ????????????? `0700`?`0600`?
- [ ] ?????? Kubernetes ???? PVC???? `emptyDir`?
- [ ] ?????? `changeId`?
- [ ] ??????? `committed`?
- [ ] ????????? `rolled-back`??? `InferNexService` ????
- [ ] ?? Agent ?????? `applied` ????????
- [ ] `rollback-failed` ??????????????
