---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: resourcescalinggroups.autoscaling.openfuyao.com
spec:
  group: autoscaling.openfuyao.com
  names:
    kind: ResourceScalingGroup
    listKind: ResourceScalingGroupList
    plural: resourcescalinggroups
    singular: resourcescalinggroup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceScalingGroup is the Schema for the resourcescalinggroups
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourceScalingGroupSpec defines the desired state of ResourceScalingGroup.
            properties:
              scalingStrategy:
                description: ScalingStrategy defines how the group scales.
                properties:
                  groupConfig:
                    description: GroupConfig defines GroupReplication behavior.
                    properties:
                      groupName:
                        description: GroupName is used as a naming prefix for cloned
                          resources.
                        minLength: 1
                        type: string
                      replicas:
                        description: Replicas is the desired number of groups.
                        format: int32
                        minimum: 0
                        type: integer
                      scaleDown:
                        description: ScaleDown controls scale down ordering.
                        properties:
                          metric:
                            description: Metric describes how to build the metrics
                              query.
                            properties:
                              aggregator:
                                description: Aggregator selects the aggregation operator.
                                enum:
                                - sum
                                - avg
                                - max
                                - min
                                type: string
                              expr:
                                description: Expr is the metric name or expression.
                                minLength: 1
                                type: string
                              type:
                                description: Type declares the metric type.
                                enum:
                                - Counter
                                - Gauge
                                - Histogram
                                type: string
                              window:
                                description: Window defines the range window for rate
                                  calculation (required for Counter/Histogram).
                                type: string
                            type: object
                          order:
                            description: Order is ascending or descending.
                            enum:
                            - ascending
                            - descending
                            type: string
                        type: object
                    required:
                    - replicas
                    type: object
                  leaderFollowerConfig:
                    description: LeaderFollowerConfig defines InplaceScaling behavior.
                    properties:
                      follower:
                        description: Follower defines follower resources.
                        items:
                          description: FollowerSpec defines a follower target.
                          properties:
                            followRatio:
                              description: FollowRatio defines the follower-to-leader
                                ratio.
                              format: int32
                              minimum: 1
                              type: integer
                            resourceName:
                              description: ResourceName references TargetResources.name.
                              minLength: 1
                              type: string
                          required:
                          - followRatio
                          - resourceName
                          type: object
                        minItems: 1
                        type: array
                      leader:
                        description: Leader defines the leader resource.
                        properties:
                          replicas:
                            description: Replicas forces leader replicas before calculating
                              followers.
                            format: int32
                            minimum: 0
                            type: integer
                          resourceName:
                            description: ResourceName references TargetResources.name.
                            minLength: 1
                            type: string
                        required:
                        - replicas
                        - resourceName
                        type: object
                    required:
                    - leader
                    type: object
                  type:
                    description: Type is the scaling strategy type.
                    enum:
                    - GroupReplication
                    - InplaceScaling
                    type: string
                required:
                - type
                type: object
              targetResources:
                description: TargetResources defines the workloads managed by this
                  scaling group.
                items:
                  description: TargetResource declares a managed workload.
                  properties:
                    name:
                      description: Name is the logical name used by scaling strategy
                        references.
                      minLength: 1
                      type: string
                    resourceRef:
                      description: ResourceRef points to the workload object.
                      properties:
                        apiVersion:
                          description: APIVersion is the API version of the target
                            workload.
                          minLength: 1
                          type: string
                        kind:
                          description: Kind is the resource kind of the target workload.
                          minLength: 1
                          type: string
                        name:
                          description: Name is the resource name of the target workload.
                          minLength: 1
                          type: string
                        namespace:
                          description: |-
                            Namespace is the resource namespace of the target workload.
                            Defaults to the ResourceScalingGroup namespace when empty.
                          type: string
                        subresource:
                          description: Subresource selects a subresource for workload
                            types that support roles.
                          properties:
                            roleName:
                              description: RoleName selects a role within a RoleBasedGroup.
                              type: string
                            type:
                              description: Type is the subresource selector type.
                              enum:
                              - RoleBasedGroup
                              type: string
                            workloadApiVersion:
                              description: WorkloadAPIVersion overrides the API version
                                used by the workload kind.
                              type: string
                            workloadKind:
                              description: WorkloadKind selects the workload kind
                                used by the role.
                              enum:
                              - StatefulSet
                              - Deployment
                              - LeaderWorkerSet
                              type: string
                            workloadName:
                              description: WorkloadName overrides the generated workload
                                name for the role.
                              type: string
                          required:
                          - type
                          type: object
                      required:
                      - apiVersion
                      - kind
                      - name
                      type: object
                  required:
                  - name
                  - resourceRef
                  type: object
                minItems: 1
                type: array
            required:
            - scalingStrategy
            - targetResources
            type: object
          status:
            description: ResourceScalingGroupStatus defines the observed state of
              ResourceScalingGroup.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentReplicas:
                description: CurrentReplicas is the number of managed groups currently
                  observed.
                format: int32
                type: integer
              lastDesiredReplicas:
                description: LastDesiredReplicas is the last desired replicas observed
                  by the controller.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the last reconciled generation.
                format: int64
                type: integer
              selector:
                description: Selector is the label selector used by the scale subresource.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.scalingStrategy.groupConfig.replicas
        statusReplicasPath: .status.currentReplicas
      status: {}
