apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "infernex-bridge.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "infernex-bridge.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "infernex-bridge.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
